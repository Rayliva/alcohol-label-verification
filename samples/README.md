# Sample labels

Externally authored test labels with machine-readable ground truth. **Not the
project's own corpus** — `corpus/` is generated by `corpus/generate.py` and is
what the accuracy numbers in the README are measured against. These are a
second, independent set, which is the point of them: ground truth we did not
write is worth more than ground truth we did.

All spirits. Re-encoded from the originals to JPEG quality 85 to keep the
repository a sensible size; the originals are PNG and roughly ten times larger.

| Directory | Count | Purpose |
|---|---|---|
| `labels/` | 31 | One case per requirement — clean baselines, field mismatches, warning defects, judgment calls, degraded photographs. Source for the seeded review queue |
| `batch/` | 25 | A drag-and-drop batch upload, drawn from a 300-label set |

`manifest.json` in each directory pairs every image against a simulated COLA
application and the verdict the tool should reach. Its `expected_verdict` values
are `PASS` / `FAIL` / `REVIEW`, which map onto this project's `pass` / `fail` /
`needs_review`.

## What these are good for, and what they are not

They carry photographic degradation — angle, glare, low light, blur, curvature —
which the rendered corpus does not, and they found a real bug on arrival: the
quality gate was reporting underexposed-but-sharp labels as blurry. See
`docs/specs/pipeline.md` 2.1.

Their expected verdicts are **not** authoritative here. At least one blurred
frame is marked `PASS` where this tool refuses it, and refusing it is correct —
the warning statement is illegible, and guessing at warning text is the one
thing this tool must never do. Disagreements are worth reading before they are
worth fixing.

The full 300-label set is not committed. Twenty-five is enough to exercise the
batch path end to end; the throughput figures in the README come from the
project's own corpus, which is reproducible.
