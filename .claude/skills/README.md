# Skills

This directory holds per-project skill files. Each `<name>.md` describes how to do one specific kind of work in this project's stack (e.g., `add-migration.md`, `run-tests.md`).

Populated by `/scaffold-skills` after `/prd` and `/tech-interview` complete.

When adding skills manually, also update `INDEX.md` so the agent can discover them.
